class: UserFolderAccess
table: user_folder_access
fields:
  userId: int, relation(parent=serverpod_user_info, onDelete=Cascade)
  folderPath: String
indexes:
  user_folder_access_idx:
    fields: userId, folderPath
    unique: true
